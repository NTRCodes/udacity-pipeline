{
  "scripts": {
    "frontend:install": "cd reactnd-contacts-complete && npm install",
    "backend:install": "cd reactnd-contacts-server && npm install",
    "frontend:build": "npm run reactnd-contacts-complete:build",
    "backend:build": "npm run reactnd-contacts-server:build"
  }
}
